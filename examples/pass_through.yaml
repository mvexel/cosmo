tables:
  all:
    # Empty filter matches everything
    filter: {}
    columns:
      - name: "tags"
        source: "tags"
        type: "json"
      - name: "meta"
        source: "meta"
        type: "json"
      - name: "refs"
        source: "refs"
        type: "json"
